# Mapping Policies

- [`modeling-choices.yaml`](modeling-choices.yaml) records approved ontology modeling patterns.
- [`iri-policy.yaml`](iri-policy.yaml) records deterministic IRI construction rules.

These files are normative project policy. A direct mapping change that conflicts with either policy requires an explicit modeling decision rather than an implementation workaround.

