# Web

Reserved for the future GitHub Pages interface that will expose reviewed canned SPARQL queries without requiring users to write SPARQL.

The first UI-facing query components and allowlisted compiler are documented in
[`query-builder/`](query-builder/README.md). They are framework-independent so a
later interface can populate select boxes directly from the exported catalog.
