# Accepted PAQ-2 release defaults

On 2026-09-08, the user selected **Use those three defaults** in response to
the explicit question preserved in [decision.json](decision.json).

PAQ-2 will display percentiles on a 0–100 scale, compare eligible MLB
regular-season plate appearances within the selected season, and retain exact
fractions internally, rounding only for display. The corresponding
[release-defaults contract](../../../sparql/metrics/paq-2-release-defaults.json)
records exactly that scope.

This is a metric-defaults decision, not an ontology proposal acceptance.
Unresolved graph and contribution/completeness decisions remain under review.
