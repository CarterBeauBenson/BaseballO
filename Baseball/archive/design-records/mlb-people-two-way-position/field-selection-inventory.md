# Field selection inventory

| Source field | Classification | Accepted use |
| --- | --- | --- |
| `primaryPosition.code` | genuinely additional reviewed code | Exact value `Y` selects the accepted composite position pattern. |
| `primaryPosition.name` | identity/join-only tuple guard | Must equal `Two-Way Player`; it does not create a class. |
| `primaryPosition.type` | identity/join-only tuple guard | Must equal `Two-Way Player`; it does not create a class. |
| `primaryPosition.abbreviation` | identity/join-only tuple guard | Must equal `TWP`; it does not create a class. |
| `people[0].id` | identity/join-only | Grounds the Person and persistent Role IRIs. |

No other people fields are newly admitted by this decision.
