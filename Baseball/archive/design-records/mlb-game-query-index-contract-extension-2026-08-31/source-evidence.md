# Source evidence

The accepted consumer-migration record requires the query index to expose
reviewed current classifications. The checked-in compiler has an explicit
predicate allowlist and required-property table, so its contract must be
extended in the same change as the CONSTRUCT components and SHACL shapes.
