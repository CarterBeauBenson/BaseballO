<!-- Generated by scripts/generate_rml_mermaid.py; do not edit. -->
<!-- Mapping SHA-256: 55c45ecc9df86d579681cfa026c088b432d3de03bbb7b6183ebb6c6fdd04c5dd -->

# Replay review core - MLB game RML

A reviewed on-field judgment produces the prior decision and leads to a distinct replay-review act that produces a replay decision; only source-described challenges create a Challenge Act or challenged-review classification.

Solid relation arrows are explicit parent-triples-map joins. Dotted relation arrows are IRI-template matches inferred by the reviewer from the actual RML templates. Cross-pattern targets are listed in the table rather than drawn, keeping the diagram small.

```mermaid
flowchart LR
    S01["Source: ChallengedReviewSource"]
    S02["Source: ReviewedPitchPlaySource"]
    S03["Source: ReviewedPlaySource"]
    S04["Source: ReviewedPlayerChallengeSource"]
    M01["ReviewOnFieldJudgmentMap<br/>Reviewed On-Field Umpire Judgment Act"]
    M02["ReviewOnFieldDecisionMap<br/>Reviewed On-Field Baseball Decision ICE"]
    M03["ReviewChallengeMap<br/>Challenge Act"]
    M04["ReviewPlayerChallengeMap<br/>Player Challenge Act"]
    M05["PitchReviewOnFieldUmpireMap<br/>relationship overlay"]
    M06["ReplayReviewActMap<br/>Baseball Replay Review Act"]
    M07["ChallengedReplayReviewActMap<br/>Challenged Baseball Replay Review Act"]
    M08["ReplayDecisionMap<br/>Baseball Replay Decision ICE"]
    S03 --> M01
    S03 --> M02
    S01 --> M03
    S04 --> M04
    S02 --> M05
    S03 --> M06
    S01 --> M07
    S03 --> M08
    M01 -.->|has output; template| M02
    M01 -.->|precedes; template| M06
    M02 -.->|is input of; template| M06
    M03 -.->|preceded by; template| M01
    M03 -.->|precedes; template| M06
    M06 -.->|has input; template| M02
    M06 -.->|has output; template| M08
    M06 -.->|preceded by; template| M01
    M07 -.->|preceded by; template| M03
    M08 -.->|is output of; template| M06
```

## Logical sources

| Source | Execution file | JSONPath iterator | Maps in this review |
| --- | --- | --- | ---: |
| `ChallengedReviewSource` | `game-context.json` | `$.liveData.plays.allPlays[?(@._baseballO.reviewInitiation == 'challenge')]` | 2 |
| `ReviewedPitchPlaySource` | `game-context.json` | `$.liveData.plays.allPlays[?(@._baseballO.reviewType == 'pitch_result')]` | 1 |
| `ReviewedPlaySource` | `game-context.json` | `$.liveData.plays.allPlays[?(@._baseballO.hasReview == true)]` | 4 |
| `ReviewedPlayerChallengeSource` | `game-context.json` | `$.liveData.plays.allPlays[?(@._baseballO.hasReviewChallengerId == true)]` | 1 |

## Triples maps

| Triples map | Subject class | Emitted relations | Cross-pattern targets |
| --- | --- | --- | --- |
| `ReviewOnFieldJudgmentMap` | Reviewed On-Field Umpire Judgment Act | has output, occurrent part of, precedes | occurrent part of -> ReviewOnFieldAdjudicatedProcessMap (template) |
| `ReviewOnFieldDecisionMap` | Reviewed On-Field Baseball Decision ICE | is about, is input of | is about -> ReviewOnFieldAdjudicatedProcessMap (template) |
| `ReviewChallengeMap` | Challenge Act | preceded by, precedes | none |
| `ReviewPlayerChallengeMap` | Player Challenge Act | has participant | has participant -> Player (template) |
| `PitchReviewOnFieldUmpireMap` | relationship overlay | has participant, realizes | has participant -> Person (template), realizes -> Umpire (template) |
| `ReplayReviewActMap` | Baseball Replay Review Act | has input, has output, occurrent part of, preceded by, precedes | occurrent part of -> ReplayAdjudicatedProcessMap (template), precedes -> BalkResultJudgmentOverlayMap (template) |
| `ChallengedReplayReviewActMap` | Challenged Baseball Replay Review Act | preceded by | none |
| `ReplayDecisionMap` | Baseball Replay Decision ICE | is about, is output of | is about -> ReplayAdjudicatedProcessMap (template) |
