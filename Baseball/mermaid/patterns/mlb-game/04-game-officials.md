<!-- Generated by scripts/generate_rml_mermaid.py; do not edit. -->
<!-- Mapping SHA-256: ef70601419120731d914fe59980ec1dbefa4ce070f2bbca8ce5b46dbea3cb112 -->

# Game officials - MLB game RML

Umpires and the official scorer bear persistent adjudicator roles that the game realizes and that participate in open role stases.

Solid relation arrows are explicit parent-triples-map joins. Dotted relation arrows are IRI-template matches inferred by the reviewer from the actual RML templates. Cross-pattern targets are listed in the table rather than drawn, keeping the diagram small.

```mermaid
flowchart LR
    S01["Source: OfficialSource"]
    S02["Source: RootSource"]
    M01["UmpirePersonMap<br/>Person"]
    M02["UmpireRoleMap<br/>Umpire Role"]
    M03["GameUmpireCareerRealizationMap<br/>Baseball Game"]
    M04["UmpireRoleStasisMap<br/>Stasis of Role"]
    M05["UmpireRoleStasisIntervalMap<br/>one-dimensional temporal region"]
    M06["OfficialScorerPersonMap<br/>Person"]
    M07["OfficialScorerRoleMap<br/>Official Scorer Role"]
    M08["GameOfficialScorerCareerRealizationMap<br/>Baseball Game"]
    M09["OfficialScorerRoleStasisMap<br/>Stasis of Role"]
    M10["OfficialScorerRoleStasisIntervalMap<br/>one-dimensional temporal region"]
    S01 --> M01
    S01 --> M02
    S01 --> M03
    S01 --> M04
    S01 --> M05
    S02 --> M06
    S02 --> M07
    S02 --> M08
    S02 --> M09
    S02 --> M10
    M02 -.->|has realization; template| M03
    M02 -.->|inheres in; template| M01
    M02 -.->|participates in; template| M04
    M03 -.->|has participant; template| M01
    M03 -.->|realizes; template| M02
    M04 -.->|has participant; template| M01
    M04 -.->|has participant; template| M02
    M04 -.->|occupies temporal region; template| M05
    M07 -.->|has realization; template| M03
    M07 -.->|inheres in; template| M06
    M07 -.->|participates in; template| M09
    M08 -.->|has participant; template| M06
    M08 -.->|realizes; template| M07
    M09 -.->|has participant; template| M06
    M09 -.->|has participant; template| M07
    M09 -.->|occupies temporal region; template| M10
```

## Logical sources

| Source | Execution file | JSONPath iterator | Maps in this review |
| --- | --- | --- | ---: |
| `OfficialSource` | `game-context.json` | `$.liveData.boxscore.officials[*]` | 5 |
| `RootSource` | `game.json` | `$` | 5 |

## Triples maps

| Triples map | Subject class | Emitted relations | Cross-pattern targets |
| --- | --- | --- | --- |
| `UmpirePersonMap` | Person | type assertion only | none |
| `UmpireRoleMap` | Umpire Role | has realization, inheres in, participates in | none |
| `GameUmpireCareerRealizationMap` | Baseball Game | has participant, realizes | none |
| `UmpireRoleStasisMap` | Stasis of Role | has participant, occupies temporal region | none |
| `UmpireRoleStasisIntervalMap` | one-dimensional temporal region | type assertion only | none |
| `OfficialScorerPersonMap` | Person | label | none |
| `OfficialScorerRoleMap` | Official Scorer Role | has realization, inheres in, participates in | none |
| `GameOfficialScorerCareerRealizationMap` | Baseball Game | has participant, realizes | none |
| `OfficialScorerRoleStasisMap` | Stasis of Role | has participant, occupies temporal region | none |
| `OfficialScorerRoleStasisIntervalMap` | one-dimensional temporal region | type assertion only | none |
