<!-- Generated by scripts/generate_rml_mermaid.py; do not edit. -->
<!-- Mapping SHA-256: 01f3efbccd0d2691d2b9ea22d2976ccca3e092a05a923e48639427281ac4ce8b -->

# Passed-ball scoring classification - MLB game RML

A passed ball is an institutional scorer judgment, decision, rule, and counted process; the feed does not independently establish a physical pitch-ball control failure.

Solid relation arrows are explicit parent-triples-map joins. Dotted relation arrows are IRI-template matches inferred by the reviewer from the actual RML templates. Cross-pattern targets are listed in the table rather than drawn, keeping the diagram small.

```mermaid
flowchart LR
    S01["Source: PassedBallRunnerSource"]
    S02["Source: RootSource"]
    M01["PassedBallProcessMap<br/>Passed Ball Process"]
    M02["PassedBallJudgmentMap<br/>Passed Ball Judgment Act"]
    M03["PassedBallDecisionMap<br/>Passed Ball Decision ICE"]
    M04["PassedBallRecordMap<br/>relationship overlay"]
    M05["PassedBallRuleMap<br/>Passed Ball Rule"]
    S01 --> M01
    S01 --> M02
    S01 --> M03
    S01 --> M04
    S02 --> M05
    M01 -.->|has occurrent part; template| M02
    M01 -.->|prescribed by; template| M05
    M02 -.->|has output; template| M03
    M02 -.->|occurrent part of; template| M01
    M02 -.->|prescribed by; template| M05
    M03 -.->|is about; template| M01
    M03 -.->|is output of; template| M02
    M04 -.->|is about; template| M01
    M04 -.->|is about; template| M02
    M04 -.->|is about; template| M03
```

## Logical sources

| Source | Execution file | JSONPath iterator | Maps in this review |
| --- | --- | --- | ---: |
| `PassedBallRunnerSource` | `game-context.json` | `$.liveData.plays.allPlays[*].runners[?(@.details.eventType == 'passed_ball')]` | 4 |
| `RootSource` | `game.json` | `$` | 1 |

## Triples maps

| Triples map | Subject class | Emitted relations | Cross-pattern targets |
| --- | --- | --- | --- |
| `PassedBallProcessMap` | Passed Ball Process | has occurrent part, occurrent part of, prescribed by | occurrent part of -> Plate Appearance (template) |
| `PassedBallJudgmentMap` | Passed Ball Judgment Act | has output, has participant, occurrent part of, prescribed by, realizes | has participant -> Person (template), realizes -> Official Scorer (template) |
| `PassedBallDecisionMap` | Passed Ball Decision ICE | is about, is output of | none |
| `PassedBallRecordMap` | relationship overlay | is about | none |
| `PassedBallRuleMap` | Passed Ball Rule | type assertion only | none |
