<!-- Generated by scripts/generate_rml_mermaid.py; do not edit. -->
<!-- Mapping SHA-256: a288d8d9e8e638468897751f8eda557afe2f40c2d055fa2acb378682b778531d -->

# In-play out contact - MLB direct RML

A swing and contact precede a fair-ball process for an in-play event whose feed call is out.

Solid relation arrows are explicit parent-triples-map joins. Dotted relation arrows are IRI-template matches inferred by the reviewer from the actual RML templates. Cross-pattern targets are listed in the table rather than drawn, keeping the diagram small.

```mermaid
flowchart LR
    S01["Source: InPlayOutSource"]
    M01["InPlayOutSwingActMap<br/>Swing Act"]
    M02["InPlayOutSwingActMapRecordAboutMap<br/>relationship overlay"]
    M03["InPlayOutContactMap<br/>Bat-Ball Contact Process"]
    M04["InPlayOutContactMapRecordAboutMap<br/>relationship overlay"]
    M05["InPlayOutFairBallMap<br/>Fair Ball Process"]
    M06["InPlayOutFairBallMapRecordAboutMap<br/>relationship overlay"]
    S01 --> M01
    S01 --> M02
    S01 --> M03
    S01 --> M04
    S01 --> M05
    S01 --> M06
    M01 -.->|precedes; template| M03
    M02 -.->|is about; template| M01
    M03 -.->|preceded by; template| M01
    M04 -.->|is about; template| M03
    M06 -.->|is about; template| M05
```

## Logical sources

| Source | JSONPath iterator | Maps in this review |
| --- | --- | ---: |
| `InPlayOutSource` | `$.liveData.plays.allPlays[?(!(@.result.eventType == 'sac_bunt'))].playEvents[?(@.isPitch == true && @.details.call.code == 'X')]` | 6 |

## Triples maps

| Triples map | Subject class | Emitted relations | Cross-pattern targets |
| --- | --- | --- | --- |
| `InPlayOutSwingActMap` | Swing Act | has participant, occurrent part of, occurs at, preceded by, precedes, realizes | has participant -> BatterPersonFromPlayMap (join), has participant -> SwingBaseballBatMap (template), occurrent part of -> BatterActMap (join), occurrent part of -> PlateAppearanceMap (join), occurs at -> Baseball Field (template), preceded by -> PitchActMap (template), realizes -> BatterRoleMap (join) |
| `InPlayOutSwingActMapRecordAboutMap` | relationship overlay | is about | none |
| `InPlayOutContactMap` | Bat-Ball Contact Process | occurrent part of, occurs at, preceded by, precedes | occurrent part of -> PlateAppearanceMap (join), occurs at -> Baseball Field (template), precedes -> BattedBallMotionMap (template) |
| `InPlayOutContactMapRecordAboutMap` | relationship overlay | is about | none |
| `InPlayOutFairBallMap` | Fair Ball Process | occurrent part of, occurs at, preceded by | occurrent part of -> PlateAppearanceMap (join), occurs at -> Baseball Field (template), preceded by -> BattedBallMotionMap (template) |
| `InPlayOutFairBallMapRecordAboutMap` | relationship overlay | is about | none |
