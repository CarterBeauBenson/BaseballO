# D1 defensive source mapping and serving proof

D1 was explicitly accepted and published in `e4166c1` before implementation.
The unchanged game 822693 source produced 31,703 triples and passed the owning
authoritative Jena SHACL profile, followed by D1's source-bound exact census.

The projection contains 20 distinct performances: 16 Catch Attempts, one
other Fielding Attempt and three Throw Acts. Each Catch Attempt is also typed
Fielding Attempt on the same individual. Actual agents realize their own
persistent Fielder Roles. There are no new ontology terms or properties.

The real PA 26 relay was proven separately through RMLMapper and SHACL before
the whole game: seven performances by four defenders, with no invented tag
or strict precedence. Nine focused tests cover source joins, corrections,
credit duplication, explicit ground/tag narratives, missing/extra graph facts
and promotion evidence tampering. Fifteen existing defensive/PAQ tie-breaker
query and SQL tests also pass. Changed PowerShell stages parse successfully.

The canonical Jena query recovered exactly all 20 acts and all 107 contact
plays. SQL retained the hash-bound admission exactly and withheld incomplete
player averages. See [query-sql.json](query-sql.json), [admission.json](admission.json)
and the complete [source census](source-census.json). These are bounded
developer proofs; they do not claim live corpus promotion.

## Remaining evidence boundaries

Thirteen simple catches have complete supported act populations. The remaining
94 contact plays remain in the census: 62 nonterminal or reviewed descriptions,
31 unsupported complete-performance descriptions, and one relay with unresolved
terminal touch and strict order. The exact counts below are governed by the
census, not inferred from absence of credits. Generic groundout A-to-B wording
still lacks the resolved throw reading required by D1. A sole putout does not
prove a tag. Ordinary foul contacts likewise do not identify all defensive acts.

The source-owned D1 stage now runs before promotion. Positive act conformance
can pass while population admission remains withheld. NiFi retains source,
RDF, implementation, shapes and report hashes, and the SQL materializer accepts
only promotion-bound proofs. Source corrections with ambiguous existing
performance alignment quarantine rather than silently reassigning identities.

The existing asynchronous August 25 recovery request remains pending in NiFi;
its 15-minute recovery worker and daily acquisition schedule are unchanged.
No healthy job was polled for completion or manually rebuilt.
