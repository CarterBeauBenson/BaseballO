# Derived-product SPARQL

Queries belong here when their primary input is a rebuildable RDF or analytical
product rather than authoritative source graphs. Existing query-index queries
retain their stable paths under `../query-index/` and are classified as
`derived` in `../source-scope-catalog.json`. New derived families may be placed
here when no established contract path exists.
