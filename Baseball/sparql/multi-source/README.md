# Multi-source SPARQL

Queries belong here only when they integrate two or more independently
promoted source modules. Each query must declare all dependencies in
`../source-scope-catalog.json`; it may not conceal source integration in RML or
SHACL. No multi-source query is active yet.
