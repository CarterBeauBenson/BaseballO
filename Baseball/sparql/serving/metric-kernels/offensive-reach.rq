# Generated by scripts/generate_metric_suite.py.
# Calculation on admitted SPARQL bindings; not a source-evidence admission rule.
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?key (SUM(IF(?progress36>0,1,0)) AS ?numerator) (1 AS ?denominator)
WHERE {
  VALUES (?key ?participant ?progress36) {
    # INPUT_ROWS
  }

}
GROUP BY ?key
