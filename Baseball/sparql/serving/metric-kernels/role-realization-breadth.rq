# Generated by scripts/generate_metric_suite.py.
# Calculation on admitted SPARQL bindings; not a source-evidence admission rule.
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?key (COUNT(DISTINCT ?roleType) AS ?numerator) (1 AS ?denominator)
WHERE {
  VALUES (?key ?act ?roleType) {
    # INPUT_ROWS
  }
  FILTER(?roleType IN ("https://baseballontology.org/BatterRole","https://baseballontology.org/BaserunnerRole","https://baseballontology.org/PitcherRole","https://baseballontology.org/FielderRole"))
}
GROUP BY ?key
