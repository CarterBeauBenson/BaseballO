# Generated by scripts/generate_metric_suite.py.
# Calculation on admitted SPARQL bindings; not a source-evidence admission rule.
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?key (SUM(IF(?runnerOnBase,?existingDestruction36,0)) AS ?numerator) (36*SUM(IF(?runnerOnBase,1,0)) AS ?denominator)
WHERE {
  VALUES (?key ?pa ?runnerOnBase ?existingDestruction36) {
    # INPUT_ROWS
  }

}
GROUP BY ?key
