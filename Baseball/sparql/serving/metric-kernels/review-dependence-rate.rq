# Generated by scripts/generate_metric_suite.py.
# Calculation on admitted SPARQL bindings; not a source-evidence admission rule.
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?key (SUM(IF(?eligible && ?reviewed,1,0)) AS ?numerator) (SUM(IF(?eligible,1,0)) AS ?denominator)
WHERE {
  VALUES (?key ?outcome ?eligible ?reviewed) {
    # INPUT_ROWS
  }

}
GROUP BY ?key
