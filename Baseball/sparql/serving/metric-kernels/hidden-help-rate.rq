# Generated by scripts/generate_metric_suite.py.
# Calculation on admitted SPARQL bindings; not a source-evidence admission rule.
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?key (SUM(IF(?batterProgress36=0 && ?otherProgress36>0,1,0)) AS ?numerator) (SUM(IF(?batterProgress36=0,1,0)) AS ?denominator)
WHERE {
  VALUES (?key ?pa ?batterProgress36 ?otherProgress36) {
    # INPUT_ROWS
  }

}
GROUP BY ?key
