# Generated by scripts/generate_metric_suite.py.
# Calculation on admitted SPARQL bindings; not a source-evidence admission rule.
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?key (SUM(IF(?score36<0,-?score36,0)) AS ?numerator) (36 AS ?denominator)
WHERE {
  VALUES (?key ?game ?episode ?empty ?score36) {
    # INPUT_ROWS
  }
  FILTER(?empty)
}
GROUP BY ?key
