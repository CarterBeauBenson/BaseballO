# Source evidence

The accepted transaction contract validates `typeCode`/`typeDesc` against a
pinned `/api/v1/transactionTypes` snapshot and already permits the exact Death
code with a non-null Person to map to CCO Death. The row does not establish a
second transaction Act or an exact instant for every role-history boundary.

No raw response was retained for this package.
